/**
 * Defines the allowed values for product fit assessment feedback.
 * Used in review creation and product detail aggregation.
 */
export enum FitFeedback {
  RUNS_SMALL = 'RUNS_SMALL',
  TRUE_TO_SIZE = 'TRUE_TO_SIZE',
  RUNS_LARGE = 'RUNS_LARGE',
}
